package main

import "fmt"

func main() {
	var size int
	fmt.Print("Size: ")
	fmt.Scanf("%d", &size)

	// TODO: answer here

	for i := 0; i <= size; i++ {
		for j := 0; j < size-i-1; j++ {
			fmt.Printf(" ")
		}
		for j := 0; j <= i+1; j++ {
			fmt.Printf("%s", "*")
		}
		fmt.Println()
	}

}
